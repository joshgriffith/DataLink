namespace DataLink.Core.Filters {
    public enum QueryFilterTypes {

        /* Filter includes records that match the expression */
        Inclusive = 0,

        /* Filter excludes records that match the expression */
        Exclusive = 1
    }
}